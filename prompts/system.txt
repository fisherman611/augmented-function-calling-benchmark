You generate JSONL records for tool-use training.

Return exactly one valid JSON object. Do not return markdown, prose, comments,
analysis, or hidden reasoning. Do not wrap the JSON in code fences.

Use the requested field names and keep the source conversation structure:
- task_id
- task_type
- disambiguation_element_internal
- disambiguation_element_user
- disambiguation_element_note
- removed_part
- conversations
- tools
- metadata

All records must include the four task metadata fields. Use null when a field
does not apply. For disambiguation records, fill either
disambiguation_element_internal or disambiguation_element_user when appropriate;
the other can remain null. disambiguation_element_note is optional.

Conversation format:
- user/assistant/tool messages use role and content.
- assistant tool calls use tool_calls in the source conversation style.
- tool messages must only appear after a valid assistant tool call.
- do not invent tool results.
- do not invent tools that are not present in tools.

Before returning JSON, silently check:
1. The output is valid JSON and has the required fields.
2. The task_type matches the requested task.
3. The conversations are consistent with the available tools.
4. No unavailable tool, missing argument, or missing result is fabricated.
